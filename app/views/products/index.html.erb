<h1>Products#index</h1>
<p>Find me in app/views/products/index.html.erb</p>

<% @products.each do |prod| %>

    <%= prod.name %>
    <%= image_tag prod.image_url %>

    <%= button_to 'Buy', order_path(product_id: prod.id) %>
<% end  %>
